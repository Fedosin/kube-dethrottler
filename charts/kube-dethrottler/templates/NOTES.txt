{{- define "kube-dethrottler.notes" -}}
Kube Dethrottler has been installed.

It is running as a Deployment monitoring PSI (Pressure Stall Information) metrics
on your Kubernetes nodes and applying/removing taints as configured.

Configuration details:
  Poll Interval: {{ .Values.config.pollInterval }}
  Cooldown Period: {{ .Values.config.cooldownPeriod }}
  Taint Key: {{ .Values.config.taintKey }}
  Taint Effect: {{ .Values.config.taintEffect }}
  Node Filter: {{ .Values.config.nodeFilter | default "(all nodes)" }}
  Leader Election: {{ .Values.config.leaderElection.enabled }}

To check the status of the Deployment, run:
  kubectl get deployment {{ include "kube-dethrottler.fullname" . }} -n {{ .Release.Namespace }}

To see the logs:
  kubectl logs -l app.kubernetes.io/name={{ include "kube-dethrottler.name" . }} -n {{ .Release.Namespace }}

{{- end }}
